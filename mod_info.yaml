# No requiredDlcIds / forbiddenDlcIds: the loader treats both as null, which is
# exactly what it converts the deprecated "supportedContent: ALL" into. The mod
# runs with or without Spaced Out and any planet pack.
minimumSupportedBuild: 509629
version: 1.0.0.0
APIVersion: 2
